import Neighborhood from './neighborhood';

// TODO: Add component tests for the neighborhood component, it's up to you what you want to test and how
describe('<Neighborhood />', () => {
    it('renders', () => {
        cy.mount(<Neighborhood />);
    });
});
